#include <stdio.h>
/**
 * main - main block
 *Description: progran that prins all single digits of base 10
 *starting from 0
 *Return: 0
 **/
int main(void)
{
	char i;

	for (i = 0 ; i < 10 ; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
